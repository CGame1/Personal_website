name: Update publications

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Skip fetch (disabled)
        run: |
          echo "Automated fetching of Google Scholar is disabled in CI."
          echo "Please run scripts/fetch_publications.py locally and commit data/publications.json when you want to update the publications file."
